# API URL
VITE_API_BASE="http://localhost:4000"

# Security (Bearer Auth)
# Note: If VITE_ADMIN_API_SECRET is left blank (and backend is blank), auth will be disabled (for local dev only).
VITE_ADMIN_API_SECRET="your_secure_random_string_here"
